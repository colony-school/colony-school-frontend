// Types
import { SubjectPeriod } from "@utils/types/subject/period";

const PeriodInfo = ({ subjectPeriod }: { subjectPeriod: SubjectPeriod }) => {
  return (
    <div className="container-surface-variant">
      Period Info
    </div>
  );
}

export default PeriodInfo;